<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Internet access for Flask calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.UI_demo"
    android:usesCleartextTraffic="true"
    android:networkSecurityConfig="@xml/network_security_config"

    tools:targetApi="28">
    <!-- AUTH -->
    <activity
        android:name=".ui.auth.LoginActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity android:name=".ui.auth.RegisterActivity" />

    <!-- DASHBOARD -->
    <activity android:name=".ui.dashboard.DashboardActivity" />
    <activity android:name=".ui.dashboard.DashBoard3" />
    <activity android:name=".ui.dashboard.BadgesActivity" />

    <!-- QUIZ / RESULTS / TIMETABLE -->
    <activity android:name=".ui.quiz.QuizActivity" />
    <activity android:name=".ui.results.ResultsActivity" />
    <activity android:name=".ui.timetable.TimetableActivity" />

    <!-- AGENTS -->
    <activity android:name=".ui.agents.Agent1Activity" />
    <activity android:name=".ui.agents.Agent2Activity" />
    <activity android:name=".ui.agents.Agent3Activity" />

</application>

</manifest>
